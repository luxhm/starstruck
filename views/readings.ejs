<%- include('partials/header', {title:"Saved Readings"}) -%>

<h2>Previous Readings</h2>
<table>
  <tr>
    <th>
      User
    </th>
    <th>
      Birthday
    </th>
    <th>
      Sun sign
    </th>
    <th>
      Moon sign
    </th>
    <th>
      Rising sign
    </th>
    <th>
      Card drawn
    </th>
    <th>
      Card positivity
    </th>
  </tr>
  <%for(let i =0; i<readings.length; i++){%>
    <% let totalPositivity = 0; %>
    <% console.log(readings[i].card.length) %>
    <% for(let i =0; i<readings[i].card.length-1; i++){%>
      <% console.log(readings[i].card.length) %>
      <% totalPositivity += tarots[readings[i].card[i]].Positivity %>
    <% } %>
    <% console.log(totalPositivity) %>
  <tr>
  <th>
    <a href="/user/<%=users[readings[i].name].name%>"><%=users[readings[i].name].name%></a>
  </th>
  <th>
    <%=users[readings[i].name].birthday%>
  </th>
  <th>
    <%=users[readings[i].name].sunSign%>
  </th>
  <th>
    <%=users[readings[i].name].moonSign%>
  </th>
  <th>
    <%=users[readings[i].name].risingSign%>
  </th>
  <th>
    <%=readings[i].card%>
  </th>
  <th>
    <%=tarots[readings[i].card[0]].Positivity%>
  </th>
  </tr>
  <%}%>
</table>
<br>

  <%- include('partials/footer') -%>
